# Configura el nombre de la app y habilita GraphiQL en /graphiql para probar consultas GraphQL.
spring:
  application:
    name: spring-graphql
  graphql:
    graphiql:
      enabled: true
      path: /graphiql
